# bASICs VM Content

Curriculum-owned examples, templates, and static documentation source for the bASICs VM.

This repository is consumed by the VM build as a pinned Nix input. Generated files such as `node_modules`, VitePress `dist`, and OpenLane `runs` are not source.
